source_dir = Example dir to track
target_dir_base = Base dir to move files to

main = {
    uni: University dir path
    work: Work dir path
}

sub = {
    math: Math dir path,
    eng: English dir path
}